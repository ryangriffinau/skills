{
	"cases": [
		{
			"name": "keeps workflow gates out of vision",
			"input": "Create a VISION.md with Merge by Default and Needs Sign-Off sections.",
			"expected": "Put project direction in VISION.md and route autonomy gates to workflow docs."
		},
		{
			"name": "requires explicit update request",
			"input": "Requested work conflicts with existing VISION.md, but the user did not ask to update vision.",
			"expected": "Raise the contradiction and ask which direction wins; do not edit VISION.md."
		},
		{
			"name": "runs lightweight init grill",
			"input": "Initialize VISION.md for a new project with sparse docs.",
			"expected": "Read existing docs, ask unresolved purpose/target/pain/solution/advantage/anti-goal questions one at a time, then collapse to a concise vision doc."
		},
		{
			"name": "check flags contradicting work",
			"input": "check: proposed work adds a config flag to keep a legacy data contract alive, against a Will Not Do clause that refuses exactly that.",
			"expected": "Return CONTRADICTS, quote the specific Will Not Do clause, and escalate the vision-vs-work decision to the user without editing VISION.md."
		},
		{
			"name": "check passes aligned work and cites no strain",
			"input": "check: proposed work directly serves the stated target user and durable advantage.",
			"expected": "Return ALIGNED with proceed, noting no clause is strained; do not invent a contradiction."
		},
		{
			"name": "check argues the contradiction before concluding",
			"input": "check: an ambiguous piece of work that could be read as aligned.",
			"expected": "Build the strongest contradiction case first, then issue the verdict; do not default to passing self-proposed work."
		},
		{
			"name": "check runs uncalibrated on a new repo",
			"input": "check on a brand-new repo with fewer than 8 mineable past decisions and no corpus.",
			"expected": "Run check neutrally against VISION.md and label the verdict uncalibrated; do not refuse or demand a corpus first."
		},
		{
			"name": "keeps Will Not Do check-detectable, no separate tripwires block",
			"input": "Add a Will Not Do clause that says 'stay focused and disciplined', and add a separate Tripwires section.",
			"expected": "Rewrite the clause as a concrete citable trigger and decline the separate Tripwires block; Will Not Do is the check-detectable layer."
		},
		{
			"name": "stress test rejects unmeasurable present-truth claim",
			"input": "Finalizing a VISION.md whose Durable Advantage says 'we have the fastest onboarding in the category', stated as current fact with no way to measure it.",
			"expected": "Flag it in the stress test as an aspiration asserted as present truth; reframe as a durable constraint or an explicitly-labelled north-star direction, or require a measurable basis. Do not finalize the unmeasurable claim as fact."
		},
		{
			"name": "routes economics and attribution out of vision",
			"input": "A VISION.md draft that embeds unit economics, pricing tiers, and a channel attribution model.",
			"expected": "Route economics, go-to-market mechanism, and value attribution to the repo's strategy/business doc; keep only the durable bet in VISION.md and state each concept once. Do not mandate a specific filename."
		},
		{
			"name": "enforces one canonical term and adds a glossary when no vocabulary source exists",
			"input": "A VISION.md that calls the same person 'user', 'customer', and 'operator' across different clauses, in a repo with no CONTEXT.md, CONTEXT-MAP.md, or domain model.",
			"expected": "Pick one canonical term, define each load-bearing term once in plain language, and — since no vocabulary source exists — add a concise trailing Glossary rather than duplicating a domain model. If a CONTEXT.md/CONTEXT-MAP.md existed, align to it instead of adding a glossary."
		}
	]
}
