# Candidate guard: <short-name>

<!--
Write-back template (SKILL.md Step 6). Instantiate for every journal `lesson` line whose
guard_matched is null. Before filing: (1) re-check references/guards.md — if an existing
guard already covers the signal, strengthen THAT guard instead of adding one; (2) if cass
is available, run `cass pack --robot "<signal>"` and attach hits as prior evidence.
File as a bead (or direct PR for one-liners) in ryangriffinau/skills.
-->

## Signal
<!-- What triage/pane/bead output shows when this failure is occurring. Concrete, greppable. -->

## Diagnosis
<!-- Root cause, one paragraph. What is actually wrong, not what it looks like. -->

## Fix
<!-- Exact commands / actions the conductor takes. Copy-pasteable, DCG-safe (no clever one-liners). -->

## Evidence
<!-- Journal pointer (repo + ts), pane capture excerpt, bead id, commit sha. Plus cass hits if any. -->

## Proposed index row
<!-- One row for the SKILL.md guard index table: | G<n> | <name> | <signal → fix, compressed> | -->
