.DS_Store
*.log
node_modules/
.env
.env.*

# bv (beads viewer) local config and caches
.bv/

# flywheel conductor runtime state (machine-local; see flywheel-conductor skill)
.flywheel/runtime/
.agents/
.ntm/
skills-lock.json

# Claude Code per-machine local settings (settings.json is the shared one)
.claude/settings.local.json
