{"observed":"allow","command":"TOOL=convex; $TOOL deploy","reason":"shell indirection is unresolved by the pre-exec regex evaluator"}
{"observed":"allow","command":"convex dev","precondition":"a production key or selector was exported on a prior command line"}
{"observed":"allow","command":"convex dev --env-file .env.production","reason":"the regex evaluator cannot inspect env-file contents"}
{"observed":"allow","command":"convex dev --deployment happy-animal-123","reason":"a deployment reference does not encode whether its deployment type is production"}
{"observed":"allow","command":"CONVEX_DEPLOYMENT=$TARGET convex dev","reason":"an inline selector whose value uses shell indirection cannot be resolved statically"}
{"observed":"allow","command":"tmux load-buffer /tmp/brief.md","reason":"a buffer loaded from a file may contain /loop text; file contents are invisible to the pre-exec regex evaluator"}
{"observed":"allow","command":"<in-process Skill('loop') / ScheduleWakeup / CronCreate inside Claude Code>","reason":"loop skills started via an agent's own in-process tools never reach the shell; DCG guards the shell boundary only - harness permission rules own that surface"}
{"observed":"allow","command":"echo \"/loop 5m fix tests\" | claude -p -","reason":"dcg evaluates pipeline segments separately; the /loop text and the claude invocation land in different segments, so no single-segment pattern can correlate them"}
{"observed": "allow", "command": "CONVEX_DEPLOY_KEY=$CONVEX_PRODUCTION_DEPLOY_KEY convex dev", "reason": "REGRESSION dcg 0.12.4: leading env assignments are stripped before pattern matching, so rules keyed on env-carried prod selectors no longer fire (denied correctly on 0.10.0; upstream ask tracked as customer-kingfield-v6id)"}
{"observed": "allow", "command": "CONVEX_DEPLOYMENT=prod:example convex dev", "reason": "REGRESSION dcg 0.12.4: leading env assignments are stripped before pattern matching, so rules keyed on env-carried prod selectors no longer fire (denied correctly on 0.10.0; upstream ask tracked as customer-kingfield-v6id)"}
{"observed": "allow", "command": "CONVEX_DEPLOY_KEY=$CONVEX_PRODUCTION_DEPLOY_KEY convex run m:doThing", "reason": "REGRESSION dcg 0.12.4: leading env assignments are stripped before pattern matching, so rules keyed on env-carried prod selectors no longer fire (denied correctly on 0.10.0; upstream ask tracked as customer-kingfield-v6id)"}
{"observed": "allow", "command": "SKIP_PREPUSH_VERIFY=1 git push origin feature", "reason": "REGRESSION dcg 0.12.4: leading env assignments are stripped before pattern matching, so rules keyed on env-carried prod selectors no longer fire (denied correctly on 0.10.0; upstream ask tracked as customer-kingfield-v6id)"}
